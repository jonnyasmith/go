# Solution Wide Instructions

## Routing — read only what the task needs, when it needs it

### Working targets

- Working on the in-memory key-value cache, TTL eviction, snapshots, or write-ahead log → `cache/AGENTS.md`
- Working on the L7 reverse proxy, load balancing, health checks, or rate limiting → `proxy/AGENTS.md`
- Working on the Redis-compatible RESP protocol engine, TCP server, or byte-level parser → `resp/AGENTS.md`
- Working on the distributed job queue, gRPC workers, retries, or backpressure → `queue/AGENTS.md`
- Working on the process/system monitor, `/proc` metrics, TUI, or process trees → `sysmon/AGENTS.md`

### This context

- Solution-wide vocabulary → docs/agents/domain.md
- System-wide decisions → docs/adr/
- Issue tracker (GitHub Issues via `gh`) → docs/agents/issue-tracker.md
- Triage labels (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix) → docs/agents/triage-labels.md
